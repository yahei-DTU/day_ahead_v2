# Experiment configuration for Exp1

# Train parameters
train_parameters:
  alpha: 0.7 # Decision threshold for classification
  train_length: 90 # Days of training
  valid_length: 7 # Days of validation
  test_length: 1 # Days of testing

# Model parameters
experiment_parameters:
  start_date: "2023-01-03" # Start date for training
  end_date: "2025-01-03" # End date for testing
